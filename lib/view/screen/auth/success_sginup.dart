import 'package:flutter/material.dart';

class successSginup extends StatelessWidget {
  const successSginup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}