import 'package:flutter/material.dart';

class successresetpas extends StatelessWidget {
  const successresetpas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}